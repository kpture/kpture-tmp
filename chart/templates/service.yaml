apiVersion: v1
kind: Service
metadata:
  name: {{ include "tls.fullname" . }}
  labels:
    {{- include "tls.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tls.selectorLabels" . | nindent 4 }}
