apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tls.fullname" . }}-test-connection"
  labels:
    {{- include "tls.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tls.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
